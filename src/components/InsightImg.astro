---
interface Props {
  insight?: "1" | "2" | "3";
  classNames?: string;
}

const { insight, classNames = "" } = Astro.props;
---

<Fragment>
  <img
    class={classNames}
    src={`/images/insight/insight-${insight}.png`}
    alt={`insight-${insight} icon`}
  />
</Fragment>
