---
import type { AFFLATUS_TYPE } from "@constants/character";
import CustomImage from "@components/CustomImage.astro";

interface Props {
  type: keyof typeof AFFLATUS_TYPE;
  variant?: "tag";
}

const { type, variant = "tag" } = Astro.props;
---

<div>
  {
    (() => {
      const assetKey = variant === "tag" ? "at" : "";
      switch (type) {
        case "INTELLECT":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-intellect.png`}
              alt="Intellect"
              class="aspect-[3/5]"
            />
          );
        case "SPIRIT":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-spirit.png`}
              alt="Spirit"
              class="aspect-[3/5]"
            />
          );
        case "PLANT":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-plant.png`}
              alt="Plant"
              class="aspect-[3/5]"
            />
          );
        case "BEAST":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-beast.png`}
              alt="Beast"
              class="aspect-[3/5]"
            />
          );
        case "STAR":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-star.png`}
              alt="Star"
              class="aspect-[3/5]"
            />
          );
        case "MINERAL":
          return (
            <CustomImage
              src={`/images/afflatus/${assetKey}-mineral.png`}
              alt="Mineral"
              class="aspect-[3/5]"
            />
          );
        default:
          return null;
      }
    })()
  }
</div>
