---
import type { CARD_TYPE } from "@constants/character";

interface Props {
  type: keyof typeof CARD_TYPE;
}

const { type } = Astro.props;
---

<div class="pt-4 max-w-32 mx-auto">
  {
    (() => {
      switch (type) {
        case "DEBUFF":
          return (
            <img src="/images/card/attr-debuff.png" alt="attr-debuff icon" />
          );
        case "BUFF":
          return <img src="/images/card/attr-buff.png" alt="attr-buff icon" />;
        case "CHANNEL":
          return (
            <img src="/images/card/attr-channel.png" alt="attr-channel icon" />
          );
        default:
          return (
            <img src="/images/card/attr-attack.png" alt="attr-attack icon" />
          );
      }
    })()
  }
</div>
